# Debug Log

Record debugging steps for course acceptance. Include AI/open-source assisted debugging when applicable.

| Date | Area | Symptom | Cause | Fix | Evidence | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-08 | Project structure | Source files were all in the project root | No repository layout yet | Split files into rtl/include/sim/mem/constraints/docs/scripts | Directory structure checked | |
| 2026-07-09 | RV32M multiply/divide extension | R-type `funct7=0000001` instructions would be decoded as existing RV32I ALU operations, and the M extension was incomplete without divide/remainder | ALU control only inspected `funct7[5]`, which is enough for ADD/SUB and SRL/SRA but not for M-extension decode | Expanded ALU op encoding to 5 bits, passed full `funct7` into `ALUController`, added `MUL/MULH/MULHSU/MULHU/DIV/DIVU/REM/REMU` execution in `ALU`, updated `misa` to RV32IM, and regenerated `mem/program.hex` with Tests 17-18 | `python3 scripts/gen_program.py mem/program.hex` prints `Result: PASS`; Icarus `tb_selfcheck` prints `RESULT: PASS (OK)` | |
| 2026-07-09 | Icarus Verilog compatibility | `iverilog` failed to elaborate `Hazard_Unit.v` because `store_hazard_mem` and `store_hazard_wb` referenced hazard wires before their declarations | Icarus is stricter about declaration-before-use than the existing source ordering | Moved store hazard assignments after `mem_hazard_rs2` and `wb_hazard_rs2` declarations without changing the Boolean logic | `iverilog -I include -g2012 -s tb_selfcheck -o sim.vvp -f scripts/rtl_files.f sim/tb_selfcheck.v` compiles; `vvp sim.vvp` passes | |
| 2026-07-09 | FPGA bring-up / RV32M DIV timing | After programming the board, UART first printed `ID=12 ACT=F6002001 EXP=FFFFFFFB FAIL` at Test 18 (`DIV -21 / 4`, expected `-5`) | The RV32M `DIV/REM` implementation is single-cycle combinational logic; on the board the original faster system clock left insufficient timing margin, and the Vivado timing report also showed no user timing constraints | Added clock constraints in `constraints/minisys_fight_constraint.xdc`, changed the board-level system clock from the prior fast divided clock to 10 MHz, and parameterized `UARTTX` baud division so Tera Term still uses 115200 8N1 | Rebuilt and programmed the board; Tera Term output ended with `OK`, confirming the full main self-test passed on FPGA | |
